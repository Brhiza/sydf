import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cc.sydf.app',
  appName: '时月东方',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
